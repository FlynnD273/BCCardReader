<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
						 xmlns:e="clr-namespace:Pokedex.Extension"
             x:Class="Pokedex.Banner">
	<Grid ColumnSpacing="0" Padding="10" BackgroundColor="{StaticResource Fire}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>

		<Rectangle BackgroundColor="White" VerticalOptions="Center" HorizontalOptions="Fill" HeightRequest="40"/>

		<Image Grid.Column="1" Source="{e:ImageResource Pokedex.Images.pikachu.png}" 
						 HeightRequest="100" HorizontalOptions="Fill" BackgroundColor="{StaticResource Fire}"/>
	</Grid>
</ContentView>