<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
						 xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="CameraTest.MainPage"
						 xmlns:l="clr-namespace:CameraTest"
						 xmlns:m="clr-namespace:CameraTest.Model">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListView ItemsSource="{Binding Cards}" RowHeight="192">
			<ListView.ItemTemplate>
				<DataTemplate>
				<ViewCell>
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<ffimageloading:CachedImage 
								Source="{Binding Image}"
								CacheDuration= "50"
								RetryCount= "3"
								RetryDelay= "600"
								DownsampleToViewSize = "true"/>
							<!--<Image Source="{Binding Image}"/>-->
							<Label Grid.Row="1" Text="{Binding Name}"/>
						</Grid>
					</ViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<Button Grid.Row="1" Text="Add Card" Command="{Binding CreateCardCommand}"/>
	</Grid>
</ContentPage>
