<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CameraTest.MainPage"
						 xmlns:l="clr-namespace:CameraTest"
						 xmlns:c="clr-namespace:CameraTest.Converter"
						 xmlns:m="clr-namespace:CameraTest.Model">
	<ContentPage.Resources>
	</ContentPage.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListView ItemsSource="{Binding Cards}" RowHeight="192" ItemTapped="ListView_ItemTapped" SelectionMode="None">
			<ListView.ItemTemplate>
				<DataTemplate>
					<l:CardViewCell/>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<Button Grid.Row="1" Text="Add Card" Command="{Binding CreateCardCommand}"/>
	</Grid>
</ContentPage>
