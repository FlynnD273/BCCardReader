<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:m="clr-namespace:HelloWorld.Model"
             mc:Ignorable="d"
             x:Class="HelloWorld.MainPage">

	<ContentPage.BindingContext>
		<m:ViewModel/>
	</ContentPage.BindingContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<!-- Place new controls here -->
		<Label Grid.Row="0" Text="{Binding TextDisplay}" TextColor="Black" FontSize="Large" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
		<Button Grid.Row="1" Text="Click to update label" HorizontalOptions="Center" VerticalOptions="End" Command="{Binding UpdateDisplayCommand}"/>
		<Entry Grid.Row="2" Text="{Binding TextInput}" VerticalOptions="Center" BackgroundColor="MediumAquamarine" Margin="0,20,0,80"/>
	</Grid>
</ContentPage>
